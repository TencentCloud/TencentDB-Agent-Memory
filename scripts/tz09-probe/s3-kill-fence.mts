/**
 * tz-09 S3 — takeover bumps the fence and the old owner's artefact is refused (Ф2).
 *
 * The scenario is implemented in `f2-fence.mts` (written with the phase it
 * belongs to); this is the S-numbered entry point the package's check table
 * names, so both commands run the same code rather than two drifting copies.
 */
import "./f2-fence.mts";
