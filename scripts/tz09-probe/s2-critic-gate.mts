/**
 * tz-09 S2 — no approving verdict, no apply (Ф4b).
 *
 * The scenario is implemented in `f4b-critic-gate.mts` (written with the phase it
 * belongs to); this is the S-numbered entry point the package's check table
 * names, so both commands run the same code rather than two drifting copies.
 */
import "./f4b-critic-gate.mts";
