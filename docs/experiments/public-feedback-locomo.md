# E3-M17：公开长对话反馈预算验证（2026-09-14）

本次为原反馈写入贡献补充公开、零模型依赖的 L0 方法验证。固定原生 MemoryCore SQLite/FTS5 与完整原文 L1，通过训练反馈选择有限检索预算；不改 Gateway，不更换基础模型，不用 QA 引用伪装写入或删除授权。

数据来自 [LoCoMo 官方仓库](https://github.com/snap-research/locomo)，固定提交 `3eb6f2c585f5e1699204e3c3bdf7adc5c28cb376`，`locomo10.json` 的 SHA-256 为 `79fa87e90f04081343b8c8debecb80a9a6842b76a7aa537dc9fdf651ea698ff4`，许可证 CC BY-NC 4.0。下载脚本保存原许可证；原始数据不纳入代码的 MIT 授权。10 段完整历史含 5882 条话语。确定性种子 20260914，审查后训练/开发/测试为 45/24/45 个问题，按 4/2/4 段对话隔离。

`prepare.py` 只保留单来源、可由文字直接支持的问题；`audit.py` 另排除 6 个时间、行为或说话者错配，不改答案、不补题。R1 运行与训练汇总已经产生，R2 修订发生在测试分数汇总前，全部修改依据原文，旧场不进入最终结论。只有一次执行助手审查，没有独立人工双审。详细修订见 [预登记](../../examples/public-memory-feedback/PROTOCOL.md)、[R1](../../examples/public-memory-feedback/PROTOCOL_R1.md) 与 [R2](../../examples/public-memory-feedback/PROTOCOL_R2.md)。

## 最终结果

| 方法 | 公开引用覆盖 | 平均文字字节 | L0 p50/p95（ms） |
| --- | --- | --- | --- |
| 原版 FTS 固定 5 条 | 27/45，60.00% | 693.84 | 0.651/1.311 |
| 锁定反馈候选（探索） | 27/45，60.00% | 693.84 | 1.458/3.378 |
| 固定 12 条对照 | 29/45，64.44% | 1649.04 | 0.739/1.437 |
| 经采用门的部署模式 | 27/45，60.00% | 693.84 | 0.671/1.771 |

最终阈值为 0，开发采用门拒绝，approved=false。候选没有提升且产生辅助检索开销；固定扩容增加 4.44 个百分点，但文字量增加 137.67%。候选与基座的四段对话差值均为 0，聚类符号翻转 p=1.00。结果证明本场没有值得采用的预算优化，不证明普遍有效或总体严格等价。

每个测试查询 5 次顺序轮换计时，效果只计 45 个问题，独立对话仍为 4。最终 1305 条记录区分 pass/fail/error；五种模式重复结果一致，大小写/空格变体一致，强制错误 45/45 返回原基座，空输入不臆造证据。模型与实验 HTTP 调用均为 0。L1 5882 条逐字回读一致，只证明装载合同，不是语义抽取正确率。

## 复现和验收

[完整命令与内部移植说明](../../examples/public-memory-feedback/README.md)。在 Windows Node 24.14.0、Python 3.12.14 上，从无 node_modules 的纯源码副本使用快照锁文件 npm ci，完成完整 npm 构建、打包以及去重 128 项检查：84 个反馈测试（含 11 个新预算边界）、34 个候选观察器测试、3 个参数隔离测试、7 个适配/评分测试。

独立副本重新执行 prepare、audit、真实 SQLite collect、fit、test 和 analyze，114 个输入/标签、策略、45 个逐问效果及稳健性输出与原最终场完全一致；时延预期不同。不需要私有镜像、用户密钥或闭源推理服务。没有宣称已复现内部编程任务或任何端到端 QA 榜单。
