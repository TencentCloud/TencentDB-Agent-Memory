# Batch B6 — P12 tests

> Wave: tdai-memory-subagents-2026-08-02. Источник правды: ТЗ §7 (21 критерий), кристалл.

## P12 — tests (complex)

- **goal**: покрыть критерии §7 ТЗ (1-21) через **vitest** (репо: vitest ^4.1.2 +
  vitest.config.ts; НЕ bun test):
  1. build rc=0 + typecheck rc=0 (tsconfig.check.json) + node --check расширения.
  2. рестарт гейтвея → /status ok (интеграционный, на тестовом порту).
  3. /memory/run dry-run: дифф показан; после apply scene/persona ≤ лимитов; отчёт.
  4. ночной триггер (a) расписание, (b) порог ≥50 — фейковое время/чекпоинт.
  5. реиндекс: полный только при перезаписи/needsReindex; дедуп НЕ триггерит полный;
     во время реиндекса recall+search пусты (не ошибка); elapsed в отчёте.
  6. офф-свитч: TDAI_MEMORY_RECALL=off → нет инъекций (вкл. persona-restore); без — как сейчас.
  7. рекурсивный гвард: PI_MEMORY_KEEPER=1 → capture-пропуск.
  8. роль-файлы + дифф в системном промте; контракт file-based diff.json.
  9. persona через task-simple: промт содержит указание; свободная саб-сессия не пишет напрямую.
  10. автоочистка: тест на фейковых файлах (см. P11a).
  11. логирование: отчёт dataDir/logs/<role>-<ts>.json; /status показывает прогон.
  12. зонд: precision@k на фейковом корпусе.
  13. typeWeights: топ-3 меняется на фейковых records.
  14. dashboard: memory_health.md существует и обновляется.
  15. фидбек: 80-char подстрока (вкл. < 80) → priority вырос.
  16. тулы: memory_search/memory_note отвечают; note пишет в L0.
  17. digest: opt-in инжекция.
  18. конфигурабельность: grep новых файлов не находит хардкодов; zod strictObject fail-loud.
  18a. single-writer гейт: load-bearing assert на no-op фабрики; seed-инвок executeSeed.
  19. trust-boundary: (a) write-путь вне apply → abort; (b) single-flight; (c) невалидный
     diff → abort; (d) чекпоинт отдельный; (e) sqlite-vec 0.1.9 + count-чек; (f) tasks-поддерево
     пусто; (g) нет выжившего keeper после timeout-kill; (h) live keeper + дети переживают
     sweep; (i) heal re-run.
  20. негативные чеки INVARIANT: L1-промт на месте; recall-knobs не изменены; capture не тронут;
     write-роуты за токеном; env-whitelist секрето-свободен; саб-сессия не зовёт /memory/apply;
     --no-skills отсутствует.
  21. fail-open: убит pi-бинар/гейтвей выключен → агент работает, warnOnce; /memory/run → 202.
- **allowed_write_paths**: `tests/` или `src/**/*.test.ts`, `vitest.config.ts`,
  `docs/specs/tdai-memory-subagents-2026-08-02/**`
- **acceptance**: `vitest run` зелёный (полный набор); typecheck/build зелёные.
- **read_context**: все модули P1-P11, vitest.config.ts, критерии §7 ТЗ.
- **non_goals**: тесты не должны писать в реальную память (~/.pi/agent-memory/tdai)
  — фейковые dataDir; не должны требовать живую саб-сессию (mock спавна).
- **escalation_triggers**: тест требует реальную БД/эмбеддер (мок на границе).

## Батч-контракт

- Executor: task-субагент, depth=complex, свой цикл.
- Коммит: `test(tdai-memory): acceptance suite for wave criteria 1-21`.
- Verify: vitest run + typecheck + build; вернуть `OK <sha>` + `FILES:` или `FAIL: reason`.
