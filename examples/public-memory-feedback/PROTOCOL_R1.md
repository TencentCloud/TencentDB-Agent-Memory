# E3-M17-R1：评分前的数据质量修订

日期 2026-09-14。原协议和原 120 个候选样本清单保留。尚未运行 MemoryCore 检索、读取任何方法分数、选择阈值或采用候选。

通读原抽样的 120 个问题、答案和引用后，发现如下明确风险：conv-26/Q005 的参考答案为 Sunday，而引用原文为 Saturday；conv-44/Q119 的物体描述依赖图片；conv-43/Q026 的部分引用来自另一说话者；若干多证据引用还含与所问事实无直接关系的话语。不能把这些引用直接视为高置信写入或效果反馈。

本修订将实验范围收紧为**长历史中的单条明确文字事实检索**。在原结构检查之后，纳入必须同时满足：

1. 原类别为 4，且恰有一个唯一可解析的证据 ID。
2. 该证据话语没有图片链接。
3. 参考答案经过大小写、标点和连续空格规范化后，长度至少 3，且作为完整词序列直接出现于引用文字中。
4. 同一答案词序列没有直接出现在问题中。

对每段对话的合格池仍使用原种子、原排序、原 12 个上限；不够则如实报告，不增补其他对话。所有原文和原答案保持不变。本规则不是对原数据作永久纠错，也不能保证答案是唯一正确答案；它减少本场监督需要的外部推断。适用范围显著收窄，不再声称覆盖多跳、时间推理或更新问答。

分集、5 个候选阈值、训练效用函数、开发采用门、基座、成本和测试方法全部保持原协议。新分集输出使用独立目录，原候选清单仍可核查。审阅由同一执行者完成，没有多人独立标注，不报告标注一致性系数。

同名说话者 John 在部分训练/开发对话出现，但没有跨集合相同原文。本方法只学习 1 个全局数值阈值，不学习名字、文本、人物画像或样本 ID；不将不同 sample_id 自动断言为已经核实身份不同的自然人。冻结测试的 4 段与训练/开发无同名说话者。
