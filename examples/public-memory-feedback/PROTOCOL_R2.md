# E3-M17-R2：逐条语义审查后的最终数据冻结

2026-09-14。R1 的 120 条原文匹配样本已完成入库/检索和训练汇总，测试检索也已经执行；测试分数尚未汇总或查看。进一步逐项检查发现，字符串相同不能排除问题中的主体和时间前提错误。`audit.py` 列明 6 项：2 项 2023/2022 日期错配、1 项“聊天”不支持“写作”、3 项说话者主体错配。

这 6 项直接从既定样本移除，**不改答案、不改证据、不补样本**。最终训练 45、开发 24、测试 45，仍为 4/2/4 段原对话。原文 5882 条不变；各方法共用同一数据。全部 120 条审查记录保存于 `label_audit.json`，执行者为单一助手，没有独立人工双审。其余条目的状态为“审查未发现错配”，不能写成无条件保证所有自然语言答案完全无歧义。

R1 的原始输入、策略与检索结果保留，但不进入最终效果统计。R2 只按同一个预先规定的训练函数重新选择一次阈值，再过原开发门，最终测试重新执行到新目录。候选集合、效用函数、成本门、基座、稳健性条件不改；不以任何 test 得分选择排除项。

本次修订暴露的方法局限本身进入结论：仅有来源 ID 合法或答案字符串出现，仍不足以宣称语义高置信。M17 的有效结论限于审查后的公开证据检索，不推导长期写入授权，也不追认完整编程任务能力。
